{
  "name": "@studed/content-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Declarative course manifest sync tool for StudEd",
  "scripts": {
    "sync": "bun run src/index.ts",
    "validate": "bun run src/index.ts --validate",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.6.0"
  }
}
