apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: studed
  labels:
    app: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: ghcr.io/warunaudara/studed-notification-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8092
          envFrom:
            - configMapRef:
                name: studed-config
            - secretRef:
                name: studed-secrets
          resources:
            requests:
              memory: "30Mi"
              cpu: "15m"
            limits:
              memory: "96Mi"
              cpu: "150m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8092
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: studed
spec:
  ports:
    - port: 8092
      targetPort: 8092
      name: http
  selector:
    app: notification-service
