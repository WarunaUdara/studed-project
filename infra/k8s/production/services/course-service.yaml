apiVersion: apps/v1
kind: Deployment
metadata:
  name: course-service
  namespace: studed
  labels:
    app: course-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: course-service
  template:
    metadata:
      labels:
        app: course-service
    spec:
      containers:
        - name: course-service
          image: ghcr.io/warunaudara/studed-course-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8083
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: studed-config
            - secretRef:
                name: studed-secrets
          resources:
            requests:
              memory: "30Mi"
              cpu: "15m"
            limits:
              memory: "96Mi"
              cpu: "150m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8084
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: course-service
  namespace: studed
spec:
  ports:
    - port: 8083
      targetPort: 8083
      name: grpc
    - port: 8084
      targetPort: 8084
      name: http
  selector:
    app: course-service
