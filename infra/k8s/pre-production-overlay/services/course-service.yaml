apiVersion: apps/v1
kind: Deployment
metadata:
  name: course-service
  namespace: studed
  labels:
    app: course-service
  annotations:
    argocd-image-updater.argoproj.io/image-list: course=ghcr.io/warunaudara/studed-course-service
    argocd-image-updater.argoproj.io/course.update-strategy: latest
    argocd-image-updater.argoproj.io/course.allow-tags: regexp:^sha-
    argocd-image-updater.argoproj.io/course.ignore-tags: regexp:^latest$
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: course-service
  template:
    metadata:
      labels:
        app: course-service
    spec:
      terminationGracePeriodSeconds: 45
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: course-service
          image: ghcr.io/warunaudara/studed-course-service:sha-9f9ecc4
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 8083
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: studed-config
            - secretRef:
                name: studed-secrets
          resources:
            requests:
              memory: "30Mi"
              cpu: "15m"
            limits:
              memory: "96Mi"
              cpu: "150m"
          lifecycle:
            preStop:
              exec:
                command: ["sleep", "10"]
          startupProbe:
            httpGet:
              path: /health
              port: 8084
            failureThreshold: 30
            periodSeconds: 2
          readinessProbe:
            httpGet:
              path: /ready
              port: 8084
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8084
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: course-service
  namespace: studed
spec:
  ports:
    - port: 8083
      targetPort: 8083
      name: grpc
    - port: 8084
      targetPort: 8084
      name: http
  selector:
    app: course-service
