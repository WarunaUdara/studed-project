import { describe, expect, it } from "vitest";
import { SearchAskModal } from "./SearchAskModal";

describe("SearchAskModal Component", () => {
  it("exports SearchAskModal function", () => {
    expect(typeof SearchAskModal).toBe("function");
  });
});
